name: todo_list
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  equatable: ^2.0.5
  go_router: ^5.2.0
  flutter_bloc: ^8.1.1
  get_it: ^7.2.0
  flutter_svg: ^1.1.6
  flutter_gen: ^5.1.0+1
  google_fonts: ^3.0.1
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.3.2
  flutter_gen_runner: ^5.1.0+1

flutter:

  uses-material-design: true

  assets:
    - assets/images/ic_logo.svg
    - assets/images/bg_onboarding_first.svg
    - assets/images/bg_onboarding_second.svg
    - assets/images/bg_onboarding_third.svg
